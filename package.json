{
  "name": "metal-vision",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 8080 --bind 127.0.0.1 --directory public",
    "test": "node --test tests/*.test.js && python3 -m unittest discover -s tests -p '*_test.py'",
    "collect": "python3 scripts/collect.py",
    "test:browser": "node tests/browser.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
